[project]
name = "quranmedialib"
version = "4.0.0"
description = "A media generation library for Quranic Arabic text and translations with customizable layouts."
readme = "README.md"
requires-python = ">=3.13, <3.14"
license = "Apache-2.0"
dependencies = [
    "Pillow<13.0.0",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.5.0",
    "pytest>=8.0.0",
    "psutil>=6.0.0",
]

[tool.pytest.ini_options]
markers = [
    "benchmark: performance benchmarks (skipped by default)",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "psutil>=7.2.2",
]

[tool.hatch.build.targets.wheel]
packages = ["src/quranmedialib"]
exclude = [
    "**/tests/**",
    "**/test_*.py",
    "**/__pycache__/**",
]

[tool.hatch.build.targets.sdist]
include = [
    "src/quranmedialib/**",
]
exclude = [
    "**/tests/**",
    "**/test_*.py",
    "**/__pycache__/**",
]

[tool.ruff]
line-length = 120
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I"]
fixable = ["ALL"]

[tool.ruff.format]
indent-style = "space"
line-ending = "lf"
skip-magic-trailing-comma = false
quote-style = "double"
